<UserControl x:Class="ModuleA.Views.ViewA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:ModuleA.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel>
        <CheckBox Content="Can Execute" IsChecked="{Binding CanExecute}" HorizontalAlignment="Left" Margin="20 20 20 10"/>
        <Button Content="Click me" Command="{Binding ClickCommand}" HorizontalAlignment="Left" Margin="20 10" Padding="5"/>
        <TextBlock Text="{Binding Title}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="48"/>
    </StackPanel>
</UserControl>
